<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Baza Książek</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
  <h1 class="mb-4">📚 Moja Baza Książek</h1>
  <a href="/add" class="btn btn-primary mb-3">Dodaj nową książkę</a>
  <ul class="list-group">
    <% books.forEach(book => { %>
      <li class="list-group-item d-flex justify-content-between">
        <span><strong><%= book.title %></strong> – <%= book.author %> (<%= book.year %>)</span>
        <span>
                        <a href="/edit/<%= book._id %>" class="btn btn-warning btn-sm">Edytuj</a>
                        <form action="/delete/<%= book._id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-danger btn-sm">Usuń</button>
                        </form>
                    </span>
      </li>
    <% }) %>
  </ul>
</div>
</body>
</html>
